<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zublinhrapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Placing name of view in the app bar -->
        <activity
            android:name=".Admin"
            android:label="@string/strAdmin"/>
        <activity
            android:name=".AddAdminUser"
            android:label="@string/strAddAdminUser"/>
        <activity
            android:name=".PendingIdeaList"
            android:label="@string/strPendingIdeas" />
        <activity
            android:name=".CommentsView"
            android:label="@string/strComments"/>
        <activity
            android:name=".ApprovedIdeaList"
            android:label="@string/strApprovedIdeas" />
        <activity
            android:name=".ReviewerIdea"
            android:label="@string/strReviewerIdeas" />
        <activity
            android:name=".EmployeeIdea"
            android:label="@string/strEmployeeIdea" />
        <activity
            android:name=".Reviewer"
            android:label="@string/strAuthorizedReviewer" />
        <activity
            android:name=".Employee"
            android:label="@string/strValuedEmployee" />
        <activity
            android:name=".UserRegisterLogin"
            android:label="@string/strUserRegister" /> <!-- Initial Main Activity Sequence -->
        <activity android:name=".UserLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
